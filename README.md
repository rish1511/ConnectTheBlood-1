# blood-connect

Project structure for Blood Connect:

- `client/` — React + RTK Query
- `server/` — Node + Express

## Setup

1. Install dependencies for `client` and `server`.
2. Configure `.env` values.

## Structure

- `client/`: frontend application
- `server/`: backend API
